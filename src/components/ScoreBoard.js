const ScoreBoard = ({ scoreDisplay }) => {

    return (
          <div className="score-board">
              <div>Score: {scoreDisplay}</div>
          </div>

    )};
    
    
    export default ScoreBoard;